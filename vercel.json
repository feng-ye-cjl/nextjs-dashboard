{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/node",
      "config": {
        "zeroConfig": true,
        "installCommand": "npm install --legacy-peer-deps",
        "framework": "next.js",
        "distDir": "out"
      }
    }
  ]
}